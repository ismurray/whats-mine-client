{{search-bar search="search"}}
<h2>Search results:</h2>
{{#if model}}
<ul>
{{#each model as |item|}}
  <h4>Item Name: {{#link-to "items.item" item}}{{item.name}}{{/link-to}}</h4>
  <ul>
    <li>Value:
    {{#if item.value}}
    {{item.value}}
    {{else}}
    [None Listed]
    {{/if}}
    </li>
    <li>Box: {{item.box.name}}</li>
    <li>Box Name: {{#link-to "boxes.box" item.box}}{{item.box.name}}{{/link-to}}</li>
  </ul>
{{/each}}
</ul>
{{else}}
<p>You have no items that match that search</p>
{{/if}}
